#pragma once

#include <random>

//extern std::mt19937 generator;

namespace RNJesus
{
	/*
	template <typename T>
	T GetRandomNumber(T a_minValue, T a_maxValue)
	{
		if (a_minValue > a_maxValue) {
			std::swap(a_minValue, a_maxValue);
		} else if (a_minValue == a_maxValue) {
			return a_minValue;
		}

		std::uniform_real_distribution<T> distribution(a_minValue, a_maxValue);

		return distribution(generator);
	}
	*/
}
